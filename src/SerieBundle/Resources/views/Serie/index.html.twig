{% extends '::base.html.twig' %}

{% block body -%}
    <h1>TOUS LES GENRES</h1>
        {% for category in categories %}
            
            <a href="{{ path('serie_show', { 'id': category.id }) }}">{{ category.name }}</a>
                       
        
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('serie_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
